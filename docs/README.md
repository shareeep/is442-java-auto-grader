# Docs Overview

This folder contains command references and project documentation.

## Coverage Recommendation

If coverage reporting is needed, the recommended setup is the JaCoCo Gradle plugin with HTML/XML reports. See `docs/commands.md` for the suggested configuration and commands.
